import { launch } from "./launch";
import { update } from "./update";
import { init } from "./init";

export const SPA = {
  init: init,
  update: update,
  launch: launch,
  events: {},
  animates: {},
  docs: {}
}
